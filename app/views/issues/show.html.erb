<div class="container heading-container">
  <h2 class="heading-text"><%= @issue.name %></h2>
  <h4 id="select-politician-issue">select a politician to view their tweets on <%= @issue.name.downcase %></h4>

    <% @politicians.each do |politician| %>
      <ul class="list-unstyled">
        <li class="politician-name"><%= link_to image_tag(politician.image, class: "politician-image")+politician.name, interest_path(@issue, :politician_id => politician.id) %></li>
      </ul>
    <% end %>


</div>

