<div class="container heading-container">
  <h2 class="heading-text">trending politicians on twitter</h2>

<ul class="list-unstyled">
  <% @politicians.each do |politician| %>
    <li id="politician-name"><%= link_to image_tag(politician.image, class: "politician-image"), politician_path(politician)  %><%= politician.name %></li>
  <% end %>
</ul>

